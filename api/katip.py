```python import google.generativeai as genai import os # 1. API Anahtarınızı Yapılandırın # Not: API anahtarınızı çevre değişkenlerinde (environment variables) tutmak en güvenli yoldur. API_KEY = "YOUR_GEMINI_API_KEY" genai.configure(api_key=API_KEY) # 2. Model Yapılandırması (Gemini 3 Modelleri) # 2026 standartlarına göre optimize edilmiş parametreler generation_config = { "temperature": 0.7, "top_p": 0.95, "top_k": 64, "max_output_tokens": 16384, # Gemini 3'ün genişletilmiş çıktı kapasitesi "response_mime_type": "text/plain", } # 3. Modelin Başlatılması # "gemini-3-pro-latest" veya "gemini-3-ultra-latest" kullanabilirsiniz. model = genai.GenerativeModel( model_name="gemini-3-pro-latest", generation_config=generation_config, # Sistem talimatları artık bu kısımda tanımlanıyor (Hata almamak için önemli) system_instruction="Sen yardımcı, profesyonel ve teknik konularda uzman bir yapay zeka asistanısın." ) # 4. Hata Yönetimi Eklenmiş İçerik Oluşturma Fonksiyonu def generate_response(prompt): try: # Gemini 3 artık multimodal (çok modlu) girişleri çok daha hızlı işler response = model.generate_content(prompt) if response.text: return response.text else: return "Model yanıt üretemedi (Güvenlik filtrelerine takılmış olabilir)." except Exception as e: # Yaygın hataları düzeltmek için yakalama (Rate limit, Auth vb.) return f"Bir hata oluştu: {str(e)}" # 5. Kullanım Örneği user_input = "Kuantum bilgisayarların 2026'daki durumu hakkında kısa bir özet ver." result = generate_response(user_input) print("--- Gemini 3 Yanıtı ---") print(result) ```
